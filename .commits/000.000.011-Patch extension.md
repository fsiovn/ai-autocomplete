# Patch extension

- `vsce publish patch`

	```sh
	Command failed: npm version patch
	npm error code 128
	npm error An unknown git error occurred
	npm error command /usr/bin/git --no-replace-objects tag -m 0.0.2 v0.0.2
	npm error fatal: tag 'v0.0.2' already exists
	npm error A complete log of this run can be found in: /root/.npm/_logs/2025-11-20T13_48_22_544Z-debug-0.log
	```

- Delete tags

	```sh
	git tag
	```

	```sh
	git tag -d 0.0.2
	git tag -d v0.0.2
	```
